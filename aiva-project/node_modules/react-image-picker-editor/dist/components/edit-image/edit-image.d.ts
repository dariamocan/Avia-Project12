import React from 'react';
import { IState } from '../../models/index.models';
import './edit-image.scss';
export interface EditImageProps {
    labels: any;
    image: string | null | undefined;
    color: string;
    initialState: IState;
    saveUpdates: Function;
}
declare const EditImage: React.MemoExoticComponent<({ labels, image, color, initialState, saveUpdates }: EditImageProps) => JSX.Element>;
export default EditImage;
